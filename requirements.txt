# Web framework + production server
Flask==3.1.3
gunicorn==23.0.0

# AI pricing model (training + prediction)
scikit-learn==1.6.1
numpy==2.0.2
pandas==2.3.3
joblib==1.5.3

# Cloud backends: Supabase (database, pgvector RAG store), Hugging Face
# (embeddings + answer generation), optional Claude. dotenv loads .env locally.
supabase==2.31.0
psycopg2-binary==2.9.12
huggingface_hub==1.8.0
anthropic==0.116.0
python-dotenv==1.2.1

# Page + message translation
deep-translator==1.11.4

# Listing photo processing (downscale before AI quality check + storage)
Pillow==11.3.0
