Flask>=3.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
deep-translator>=1.11

# Cloud backends: Supabase (database, auth, pgvector RAG store) + Hugging Face
# (embeddings and answer generation). python-dotenv loads the .env config.
supabase>=2.4
psycopg2-binary>=2.9
huggingface_hub>=0.25
python-dotenv>=1.0
anthropic>=0.40
